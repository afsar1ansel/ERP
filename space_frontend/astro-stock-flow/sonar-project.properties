sonar.projectKey=astro_stock_flow
sonar.projectName=Astro Stock Flow
sonar.projectVersion=1.0

# Source directory (root)
sonar.sources=.

# Exclusions: files and folders that should not be analyzed
sonar.exclusions=**/node_modules/**,**/.git/**,**/venv/**,**/dist/**,**/build/**,**/coverage/**,**/tests/**,**/test/**,**/results/**,**/*.json,**/*.yml,**/*.yaml,**/*.md,**/Jenkinsfile

# Encoding
sonar.sourceEncoding=UTF-8
