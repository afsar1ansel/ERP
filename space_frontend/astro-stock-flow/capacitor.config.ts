import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.space_luggage.app',
  appName: 'space_luggage',
  webDir: 'dist'
};

export default config;
